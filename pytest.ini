[pytest]
# `from src.taxonomy import ...` needs the repo root importable. Running
# `python -m pytest` puts it on sys.path implicitly, but a bare `pytest` does
# not — which is how CI invokes it, and why the suite passed locally while
# failing on the first CI run. Setting it here makes both paths identical.
pythonpath = .
testpaths = tests
